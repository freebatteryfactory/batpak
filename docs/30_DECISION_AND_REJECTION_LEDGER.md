---
status: AUTHORITATIVE
contract_id: BP-DECISION-LEDGER-1
authority_scope: KEEP, LOCK, KILL, SUPERSEDE, DEMOTE, DEFER, and implementation dispositions
supersedes: BatPak clean-room Pass 1 and selectively retained Pass 2 rulings
last_reconciled: 2026-07-13
reconciliation_epoch: cleanroom-v1
---

# Decision and Rejection Ledger

`spec/dispositions.rs` owns decision membership, order, class, gates, disposition, subject, and final ruling. This document is the human view — the tag legend and every row below are one generated projection of the typed owner:

<!-- DECISION-LEDGER:BEGIN generated from spec/dispositions.rs by bootstrap/project.py; do not edit -->
| Tag | Meaning |
| --- | --- |
| KEEP | retained as current architecture |
| LOCK | retained and frozen against casual reopening |
| KILL | forbidden from the clean target |
| SUPERSEDE | old meaning survives through a named successor |
| DEMOTE | retained only for compatibility, oracle, or evidence |
| DEFER | outside V1; requires a real adopter and new ruling |
| OPEN-IMPLEMENTATION | owner and law fixed; exact constant selected at named gate |
| RETAIN-AS-EVIDENCE | historical material may be consulted, never treated as law |

| ID | Tag | Class | Gates | Subject | Final ruling |
| --- | --- | --- | --- | --- | --- |
| DEC-001 | LOCK | HistoricalReceipt |  | Pass 1 machine-centered architecture | Base of final v1 |
| DEC-002 | RETAIN-AS-EVIDENCE | HistoricalReceipt |  | Pass 2 package-stratified architecture | Useful ideas salvaged individually; package map rejected |
| DEC-003 | KILL | Architecture | G2 | Separate storage-format package | .fbat remains owned by batpak |
| DEC-004 | KILL | Architecture | G5 | Standalone runtime VM package | PakVM and Bvisor live inside SyncBat |
| DEC-005 | SUPERSEDE | Naming | G0 | VPak as machine name | PakVM is machine; .vpak is package extension |
| DEC-006 | LOCK | Architecture | G5 | SyncBat internal shape | runtime, pakvm, bvisor, world, port |
| DEC-007 | KILL | Architecture | G0 | Universal type directory | Root concept file + same-name directory |
| DEC-008 | KILL | Architecture | G0 | Universal tables/systems/ports/kernels tree | Put mechanics under the concept they serve |
| DEC-009 | SUPERSEDE | Architecture | G0 | HostBat | WorldImage, ProgramImage, lifecycle events, ProcessContract, NetBat |
| DEC-010 | SUPERSEDE | Architecture | G5 | Bvisor OS backend product matrix | Capability/attempt membrane over PakVM |
| DEC-011 | KEEP | Architecture | G2 | .fbat | BatPak append-oriented source-truth format |
| DEC-012 | KEEP | Architecture | G5 | .vpak | Immutable WorldImage package format |
| DEC-013 | KEEP | Architecture | G4 | BatQL compiler package | Independent frontend over BatPak image contracts |
| DEC-014 | LOCK | Architecture | G3 | TestPak role tree | Role folders; proof classes are metadata |
| DEC-015 | LOCK | Architecture | G3 | Muterprater scope | Mutation testing only inside TestPak, across exactly three frozen lanes: SemanticIr mutates BatPak-owned semantic structures and needs no per-candidate Rust compile while the reference interpreter executes the mutant and an independent evidence route judges it, SelectableCompiled compiles one test-profile shard holding many candidate slots where one stable mutation identity selects one slot per isolated process and activation is recorded as evidence rather than assumed, and CompilerBacked runs implementation-sensitive candidates under real rustc semantics without claiming a homegrown evaluator equals rustc; Muterprater owns mutation identity discovery typed plan lane selection test selection activation evidence result classification freshness receipts and promotion proposals while nextest owns isolated test-process execution and rustc owns Rust compilation semantics and compiler-backed differential tooling stays an independent backend until a qualification receipt admits its retirement; every planned mutation reaches one of the eight terminal MutationResult categories and nothing silently leaves the denominator; candidates land under target/muterprater/candidates as disposable derived material with no canonical direct-write path into tracked source, and promotion is a separate admitted action requiring an independent oracle a named invariant or documented proof gap a killed real semantic mutant or equivalent hostile evidence and an auditable proof receipt; the typed promotion denominator in spec/promotion.rs is IndependentEvidenceRoute NamedProofTarget QualifiedHostileEvidence and AuditablePromotionReceipt |
| DEC-016 | KEEP | Enforcement | G0 | Status/supersession metadata | Required on all docs |
| DEC-017 | KEEP | Capability | G6 | Bounded push + durable pull | Core delivery doctrine |
| DEC-018 | KEEP | Capability | G2 | K3 | Truth/decision handling only where unresolved data matters |
| DEC-019 | KEEP | Capability | G4 | NC¹ circuit lowering | Eligible bounded decision fragments only |
| DEC-020 | DEMOTE | Compatibility | G2 | Serde/MessagePack | Interop and historical readers, not new semantic authority |
| DEC-021 | DEMOTE | ImplementationPosture | G6 | Flume | Private reference mechanism during topology-by-topology succession |
| DEC-022 | DEMOTE | ImplementationPosture | G2 | UUID crate | Differential oracle until owned generator closes |
| DEC-023 | DEFER | ImplementationPosture | G5 | Arbitrary native/Wasm guest execution | Future external-effect adapter only after real adopter; reopening requires all of: an exact exec-time descriptor set with one declared lifecycle per descriptor and no best-effort bucket; no best-effort authority-bearing setup transition; argv and environment are never launcher authority; an authenticated launcher descriptor topology; bounded content-addressed capture with atomic publication that honestly states which streams were published; capture and report reconciliation after ambiguity; and no ambient argv env or descriptor widening (issues #218 #231 #232, superseded with full mapping) |
| DEC-024 | KEEP | Capability | G5 | Artifact/content plane | Concern and port; no public bundle extension yet |
| DEC-025 | KEEP | Architecture | G7 | Thin CLI package | Binary adapter only, no semantic ownership |
| DEC-040 | LOCK | Architecture | G0/G5 | BatPak and SyncBat semantic profiles | no_std + alloc; host mechanisms stay in explicit std/browser adapters |
| DEC-041 | LOCK | Architecture | G0 | Cargo package batpak | The real semantic and durable library, not an empty umbrella; repository and library share the product name |
| DEC-042 | DEFER | ImplementationPosture | G2 | BatDenseRecord representation | Requires a sealed-image or measured-kernel adopter; not part of the initial native authority path |
| DEC-043 | LOCK | Architecture | G0 | Examples package batpak-examples | Public-surface witness at examples/; publish is false; owns no semantic law; no TestPak dependency; production APIs only; observable output; executed through TestPak orchestration |
| DEC-044 | LOCK | Architecture | G3 | TestPak sole corpus and fixture home | crates/testpak/corpus and crates/testpak/fixtures own frozen assets; root corpus/ and fixtures/ are forbidden; no canonical package-local mirrors |
| DEC-045 | LOCK | Architecture | G3 | TestPak binary target | The testpak package ships src/lib.rs and src/bin/testpak.rs; the root justfile projects cargo run -p testpak; no separate command crate |
| DEC-046 | LOCK | Architecture | G0 | Workspace version train | 1.0.0-alpha.1 implementation train; publishable production packages ship lockstep to 1.0.0; versions below the train are rejected |
| DEC-047 | LOCK | Architecture | G0 | Default feature behavior | batpak and syncbat default to std with useful native usability; no_std via default-features false; std must not activate the threaded browser encryption mapping or interop adapter zoo |
| DEC-048 | LOCK | HistoricalReceipt |  | Orphan clean-room cutover | cleanroom is orphan with zero parents; legacy/main owns the final legacy source lineage; legacy-pre-cleanroom is the immutable legacy tag; no source merge graft cherry-pick or subtree import crosses; behavior crosses only through LEG obligations and clean witnesses; legacy source 6c4a46f8; clean-room base ada284a0 |
| DEC-049 | LOCK | Capability | G5 | Declared WorldImage entrypoint invocation | InvokeEntrypoint executes a ProgramImage bound by an admitted WorldImage entrypoint; persistent deployment; ASK or DO; effects only when the entrypoint contract declares them; capability from the WorldImage grant; a request cannot substitute a foreign ProgramImage while keeping entrypoint authority; identity binds WorldImageId WorldInterfaceHash EntrypointId ProgramImageId InvocationId AttemptId |
| DEC-050 | LOCK | Capability | G4/G5 | Ephemeral query ProgramImage invocation | ExecuteQueryProgram executes a client-supplied canonical query-only ProgramImage ephemerally under a server-issued read-only query capability envelope; effects process installation and lifecycle hooks forbidden; the server validates the image independently and client compilation grants no trust; response binds ProgramImageId query authority digest historical cut source generation completeness proof disposition actual work result digest and receipt; ALLOW DENY DEFER may be data but never drive an effect |
| DEC-051 | LOCK | Architecture | G4 | Source compilation and raw-BatQL authority boundary | BatQL source text is authoring input never executable authority; PakVM executes validated ProgramImages not source; ordinary NetBat permits InvokeEntrypoint and ExecuteQueryProgram and rejects CompileAndExecuteRawBatQL EvalText and ExecuteSource; source compilation is a separately admitted dev and admin CompilerPort not available to guests not implied by NetBat access and absent from the default server profile; it emits ProgramImage plus CompilationReceipt through the ordinary validation and admission path so source identity is provenance not runtime authority |
| DEC-052 | LOCK | Architecture | G2 | Integrity role separation | Checksum32 ContentDigest CommitmentDigest MacTag Signature and ExternalAnchor are six distinct types; a checksum never implies authority; an unkeyed digest never authenticates its own expected value |
| DEC-053 | LOCK | Architecture | G2 | Secret authority vocabulary and no homemade crypto | batpak::secret owns KeyScope KeyId KeyGeneration KeyBackendId KeyState ShredTransition RotationId RewrapId and AntiRollbackPolicy; encrypted payloads require a qualified AEAD; entropy enters only through EntropyPort; no homemade cryptographic primitives; raw secrets never enter .fbat .vpak WorldImage ordinary receipts proof manifests logs Debug or Display |
| DEC-054 | DEFER | ImplementationPosture | G2 | Password-derived key material | V1 accepts no human password or passphrase as key material; no password KDF is part of the V1 runtime contract; a KDF enters only if a later explicit password-derived-key feature earns one through the amendment path |
| DEC-056 | LOCK | Enforcement | G3/G9 | Public API ownership and semver | TestPak owns public API baselines semver classification MSRV qualification and target profile qualification; every publishable package gets a generated baseline dependency-type leakage scan semver classification downstream compile fixture and deprecation disposition; a public removal requires successor migration example deprecation interval downstream audit decision receipt and baseline update; delete-and-discover public API change is refused |
| DEC-057 | LOCK | Enforcement | G3 | CI lanes versus proof ownership | TestPak owns what must be proven; CI chooses where and when the lanes execute; release consumes fresh matching receipts; exact GitHub Actions filenames and job layouts are a projection of TestPak lane policy not authority |
| DEC-058 | LOCK | Enforcement | G9 | Release seal and refusal | A release receipt binds every field of the typed ReleaseSealField inventory in spec/architecture.rs, including a kernel qualification set that states no kernels admitted when empty; a release is refused on an unclassified denominator row a stale receipt an unresolved semver classification a compatibility reader without a removal receipt or a package-content license or SBOM gap; the library crate is batpak and the product binary is batpak-cli |
| DEC-059 | LOCK | Compatibility | G2 | Compatibility scope | Compatibility applies to durable legacy data and intentionally preserved public contracts; it does not require keeping superseded internal crate topology HostBat a standalone Bat VM a separate storage-format package an OS-backend Bvisor matrix or dual execution models alive; semver is not a hostage negotiator for dead architecture |
| DEC-060 | LOCK | Capability | G4 | Typed arithmetic and rounding law | BatQL arithmetic is dimensionally typed with spec/operators.rs OperatorSpec as the concrete operator fact authority: OperatorId and OperatorId::ALL are the closed operator identity, OperatorWordSurface and OperatorWordSurface::ALL the closed word surfaces, OperatorSymbolSurface and OperatorSymbolSurface::ALL the closed symbol surfaces, and OperatorSyntax the closed source-shape law; arithmetic is symbol-only with canonical symbols * / + -, comparisons are canonical word phrases with the exact V1 symbol aliases IS with =, IS NOT with !=, IS LESS THAN with <, IS AT MOST with <=, IS MORE THAN with >, IS AT LEAST with >=, and NOT AND OR are word-only; same-unit addition and subtraction; multiplication requires a dimensionless operand; division of like dimensions yields Ratio; Money times Money and cross-currency operations are rejected; every scale-reducing conversion names one of six canonical rounding modes HalfEven HalfAwayFromZero TowardZero AwayFromZero Floor Ceiling, and surface spellings HALF_EVEN HALF_UP DOWN UP lower to the first four; overflow division-by-zero and unsupported scale yield INVALID; illegal units are a compile-time type error; currency conversion needs an explicit function and a receipted rate source; the exact-versus-approximate authority boundary is owned by DEC-069 and docs/37 |
| DEC-061 | LOCK | Capability | G2 | Deterministic HLC query ordering and range law | HLC is causal chronological evidence never journal progress authority; ORDER BY HLC lowers to a total key HLC then GlobalSequence within one journal and HLC then StoreId then GlobalSequence across journals; DURING HLC ranges are half-open; HLC is forbidden for durability coverage cursor resume frontier advancement commit identity deadline measurement and implicit causation; when causal HLC is absent the result is Unavailable and ObservedWallTime is never promoted into Hlc |
| DEC-062 | LOCK | Capability | G4 | Kernel identity and binding policy | FROM KERNEL refers to a canonical KernelManifest never a display name Rust path or function pointer; KernelContractId KernelInterfaceHash KernelImplementationId and KernelQualificationReceiptId are distinct; WorldImage binding is either ExactImplementation pinning KernelImplementationId or QualifiedInterface pinning KernelContractId plus KernelInterfaceHash with an accepted qualification receipt; PakVM never resolves a kernel by string name and the AttemptReceipt records the exact KernelImplementationId used |
| DEC-063 | LOCK | ImplementationPosture | G2/G8 | V1 compression posture | V1 .fbat authority frames and .vpak canonical sections are uncompressed; large payloads use ArtifactRef; derived tiles may use an explicit versioned compression profile; transparent compression is forbidden; a future compressed profile requires CompressionId canonical parameters bounded decompression a maximum expansion ratio a digest-coverage law compatibility rows hostile fixtures and a measured adopter and must state whether identity covers compressed bytes uncompressed bytes or both; no compression algorithm is selected now |
| DEC-064 | LOCK | Architecture | G2 | Version identities remain distinct | BatQlLanguageVersion ProgramImageVersion WorldImageVersion PakVmIsaVersion FbatFormatVersion BatTaggedRecordVersion FrameVersion NetBatProtocolVersion KernelManifestVersion ReceiptSchemaVersion SchemaVersion LayoutVersion and Tier0QualificationArtifactVersion are distinct types; FrameVersion versions the EventFrame envelope and never the .fbat container, the payload record codec, or NetBat transport framing; FbatFormatVersion versions the .fbat journal container and its open and compatibility law; BatTaggedRecordVersion versions the tagged payload-record envelope and codec inside the encoded payload boundary; LayoutVersion versions physical organization under LayoutId and never schema meaning; Tier0QualificationArtifactVersion versions the line-oriented bootstrap Tier 0 qualification evidence artifact that bootstrap/receiptcheck.rs verifies independently and is never a ReceiptSchemaVersion, a ReleaseSeal schema, or a product ReceiptId; no generic Version crosses subsystem boundaries; canonical bytes carry their owning version; ProgramImageId commits to canonical bytes including version and is independent of compiler provenance so identical bytes from two qualified compilers share an id; NetBat protocol negotiation is independent of PakVM ISA and WorldImage versions |
| DEC-065 | LOCK | Architecture | G0/G5 | no_std and alloc realization matrix | batpak and syncbat realize their no_std plus alloc semantic surfaces not merely assert them; std and browser adapters own filesystem mapping threads sockets clock entropy keychain and web APIs; default-features false is compile-proven and a green default build is not sufficient evidence; collections use Vec bounded arenas BTreeMap ordered maps and sorted catalogs while hash maps are derived caches whose iteration never influences canonical identity formatting diagnostics execution order or receipts; the qualification matrix covers batpak no_std syncbat no_std native std default threaded browser encryption and interop; every qualified profile preserves program semantics so the same program over the same inputs yields the same canonical observables on native and browser alike, with an adapter supplying mechanism and evidence only and profile-owned differences confined to mechanism performance and available capability never to program-observable results |
| DEC-066 | LOCK | Capability | G5/G6 | Typed resource exhaustion and no-partial-publication | BudgetExceeded and ResourceExhausted are distinct outcomes never collapsed into each other a string or INVALID; INVALID means a value violated its contract while resource exhaustion is an execution failure; every attacker-influenced allocation decodes the declared length checks arithmetic enforces the semantic bound try_reserves initializes and publishes only after completion; failure never panics aborts wraps or leaves a partial value event artifact or checkpoint; this covers .fbat .vpak BatQL PakVM arenas group and match state NetBat framing proof bundles artifact staging and tile materialization |
| DEC-026 | LOCK | Architecture | G2 | Immutable accepted history | Corrections are later events |
| DEC-027 | LOCK | Compatibility | G2 | Read-time schema evolution | Historical bytes remain unchanged |
| DEC-028 | LOCK | Architecture | G6 | Turn versus Attempt | Logical and physical execution identities remain distinct |
| DEC-029 | LOCK | Architecture | G5/G6 | Runtime/Bvisor restart split | Runtime decides legality; Bvisor performs mechanics |
| DEC-030 | LOCK | Capability | G2 | Availability axes | Value state, truth, decision, completeness, freshness, proof are separate; numeric sorts exact approximate and interval are orthogonal to these axes per DEC-069 and docs/37 |
| DEC-031 | LOCK | Capability | G2 | HLC scope | Chronology/filtering aid, not durability or universal order |
| DEC-032 | KEEP | Architecture | G8 | ECS/data orientation | Implementation algebra, not ontology |
| DEC-033 | KEEP | ImplementationPosture | G8 | SIDX successor | First earned column tile |
| DEC-034 | KEEP | Capability | G2 | One evaluation, many projections | Value/explanation/evidence/margin/cost share typed evaluation; numeric evaluation projects value margin evidence and explanation per DEC-069 and docs/37 |
| DEC-035 | OPEN-IMPLEMENTATION | ImplementationPosture | G2 | EventFrameV2 field IDs and exact bytes | G2 |
| DEC-036 | OPEN-IMPLEMENTATION | ImplementationPosture | G5 | .vpak section and PakVM opcode numbers | G5 |
| DEC-037 | OPEN-IMPLEMENTATION | ImplementationPosture | G5/G7 | First persistent browser adapter | G5/G7 |
| DEC-038 | OPEN-IMPLEMENTATION | ImplementationPosture | G3/G8 | Performance/work thresholds | owning gate + TestPak |
| DEC-039 | DEFER | ImplementationPosture | G2 | Public packed-artifact extension | real adopter required |
| DEC-055 | OPEN-IMPLEMENTATION | ImplementationPosture | G2 | Exact cryptographic constants | G2 |
| DEC-067 | LOCK | Enforcement | G0 | Bootstrap lexical debt scan and generated lint posture | seedcheck lexically scans production source under crates apps and examples after stripping comments and strings for the panic unwrap expect and dbg tokens while test-owned paths tests benches fixtures corpus and fuzz are exempt; the generated workspace denies wildcard_enum_match_arm cast_possible_truncation cast_sign_loss missing_errors_doc large_enum_variant clone_on_ref_ptr and needless_pass_by_value but never globally denies expect_used and never blanket-bans as casts; pointer and layout casts belong to the compiler-assumption ledger and the TestPak AST gate; the admitted ledgerable compiler-assumption kind for pointer and integer-pointer crossings is PointerProvenance while narrowing numeric casts remain hard findings and representation-layout assumptions are not admitted until they earn a distinct detector and witness |
| DEC-068 | LOCK | Enforcement | G1/G3 | TestPak AST enforcement gate | The authoritative Rust classification is a TestPak AST gate implemented at G3 that detects function and closure-local domain types unledgered unsafe pointer and narrowing casts lint suppressions production panic unwrap expect and dbg dependency-owned public types drawer modules stale aliases and canonical-order dependence on hash-map iteration; production violations are hard findings while test-only contextual expect is permitted and dangerous mechanisms require a compiler-assumption ledger entry; the admitted ledgerable compiler-assumption kind for unsafe blocks functions and impls is UnsafeMemoryContract; the bootstrap lexical scan is defense in depth and the gate does not exist yet |
| DEC-069 | LOCK | Capability | G2/G3/G5 | Exact Authority and Qualified Approximation | Ordinary unqualified IEEE floats are rejected as authority values; first-class Qualified Approximation is accepted only with explicit FloatFormatId classification provenance and interval or error evidence and crosses into authority only through a receipted Quantize or IntervalDecision; Fixed128 a signed i128 coefficient with explicit scale unit and numeric profile is the canonical inline fixed-decimal authority representation while ExactInteger and ExactRatio retain their own canonical exact forms; wide exact is a qualified profile seam not ordinary inline V1 authority; approximation crosses to authority through QuantizePoint to a fixed-decimal point or QuantizeInterval to an enclosing exact interval never through a silently invented point; this refines DEC-030 DEC-034 and DEC-060 and is owned by docs/37 |
| DEC-070 | LOCK | Enforcement | G0 | Guarantee classification and derived Guarantee Graph | SEED LEG DEC architecture and qualification facts retain their native authority and schemas; guarantee classification exposes their kind lifetime ownership gates witnesses failure posture and explicit relations through a generated derived view; the Guarantee Graph is a derived structural index not normative authority and cannot replace or silently revise an owning fact; the locked fork is separate authored fact families plus one generated derived graph rather than one hand-authored universal ledger or several unrelated ledgers with no lifecycle relationship; lifetime active-or-closed status compatibility disposition and deletion condition remain orthogonal |
| DEC-071 | LOCK | Capability | G2/G9 | Authenticated-history profiles and witness policy | V1 exposes InternalConsistency SignedHistory and ExternallyAnchoredHistory as explicit authenticated-history profiles; witness policy is typed and constrained by profile so Required appears only with ExternallyAnchoredHistory and InternalConsistency permits only None; required external anchoring fails closed on an absent stale conflicting unverifiable cryptographically invalid lineage-mismatched generation-mismatched or accumulator-mismatched witness; internal consistency and signed history alone do not prove that the selected generation is the newest history ever acknowledged because that freshness claim requires an independent monotonic witness; an optional witness is optional to supply but once supplied is mandatory to validate and a supplied invalid witness refuses rather than degrading to absence; no verification result receipt release or public claim may assert stronger integrity authenticity freshness or rollback resistance than the selected profile and verified witness posture support; the typed matrix is owned by spec/architecture.rs and the threat and claims by docs/19 |
| DEC-072 | LOCK | Enforcement | G0/G9 | Typed decision classification and gate binding | Every decision declares one typed DecisionClass; every implementation-bearing decision names at least one typed implementation or qualification GateId through the single spec/gates.rs identity; Naming and HistoricalReceipt decisions may remain ungated; bootstrap audit and seedcheck reject missing duplicate unknown noncanonical documentary-mismatched or class-incompatible gate bindings; documentary decision views and Guarantee Graph decision metadata must equal the typed DecisionSpec facts; the class not the title ID range document section or keyword decides whether a gate is required |
| DEC-073 | LOCK | Capability | G4/G5/G8/G9 | Adaptive residual specialization | PakVM's reference interpreter remains the executable semantic authority for admitted programs; V1 may derive a SpecializedPlan through compile-time or bounded first-use residual specialization over explicitly identified static facts; every SpecializedPlan is content-addressed bound to a complete specialization key independently verifiable against that key and treated only as derived optimization material; a missing refused expired corrupt incompatible or mismatched residual plan is discarded and execution falls back to the reference interpreter; deleting every specialization cache changes performance and WorkObservation only never meaning authority proof posture effect legality or receipt semantics; first-use specialization is bounded by an explicit work budget and the absolute monotonic deadline executes no application effect and never bypasses Bvisor admission; specialization may discharge only checks proven static under the complete key and recorded as discharged; the scalar Column SelectionMask ScanKernel and LateMaterialization path is the reference behavior and no optimized kernel is the sole oracle; V1 does not require native-code generation executable memory deoptimization machinery nightly Rust unsafe intrinsics or a SIMD runtime dependency; this refines DEC-032 and DEC-033 and adds no public BatQL syntax |
| DEC-074 | LOCK | Enforcement | G3/G9 | Proof-policy anti-weakening | TestPak owns one explicit proof-policy anti-weakening gate; every change to an owned proof-policy surface declares one typed ProofPolicyChangeClass of Strengthening Neutral or Weakening and an unclassified proof-policy change is refused; a Neutral classification requires parity evidence because a refactor is not automatically neutral; a Weakening requires explicit DEC or supersession authority rationale affected guarantee references the old and new boundary a replacement-proof or accepted-risk disposition hostile qualification proving the new boundary is enforced release-visible disclosure and an auditable receipt, and a generic issue link is not decision authority while a commit message is not a weakening receipt; the gate does not claim to infer semantic truth from arbitrary source diffs but proves that proof-policy changes are explicitly classified authorized qualified and receipted rather than entering as invisible cleanup; no test mutation waiver threshold freshness selection promotion or release-blocking policy becomes weaker merely because its implementation was reorganized; weakening the anti-weakening gate itself requires the same complete authority package; changes to the spec/promotion.rs PromotionRequirement denominator classify under ProofPolicySurface::CandidatePromotion |
| DEC-075 | LOCK | Architecture | G2/G5/G6 | Single-writer dual-axis reconciliation | One writer establishes exact durable order through GlobalSequence and CommitPoint; Hlc and ObservedWallTime carry chronology causal evidence lag and temporal pruning only and are never commit retry deadline checkpoint or cursor authority; TurnId binds the frozen input cuts and is stable on replay while every physical execution carries a fresh AttemptId; one admitted evaluation over one frozen turn produces both the semantic projection and the evidence projection and production never re-evaluates the expression to construct its receipt; every attempt response is bound to its request attempt world generation capability deadline and logical operation; durable effect intent and attempt evidence survive loss of an in-memory waiter; retry resume compensation or terminal refusal is selected only from the declared recovery class durable intent attempt evidence commit knowledge receipt completeness overall monotonic deadline and runtime-owned restart authorization, and elapsed wall time process death a missing acknowledgement or a missing in-memory waiter is never sufficient; reconciliation appends or references new evidence and never rewrites the original event attempt observation or receipt; a host driver may block poll suspend or await without changing logical traces identities receipts or recovery dispositions; one name never owns multiple ordering authorities; spec/reconciliation.rs owns the closed reconciliation bindings and docs/02_SYSTEM_MODEL.md owns the end-to-end law |
| DEC-076 | LOCK | Enforcement | G0 | Cross-run same-source corroboration and promotion confirmation | Two independent hosted Tier 0 qualification runs corroborate the SAME committed source snapshot when and only when they agree on the deterministic source-identity coordinates source commit source tree spec-manifest digest hosted-workflow digest and the independently-recomputed materializer output-tree; a candidate-branch run happens before promotion so same-source names a committed source snapshot not yet a promoted one; the current Tier 0 contract does not require cross-run executable-byte identity and each run independently binds its own executables so a differing compiled-artifact executable digest is never a same-source or promotion divergence and a later reproducible-build law may add executable equality as stronger corroboration without redefining today's source identity; a differing toolchain or physical target does not defeat same-source and is recorded only as orthogonal corroboration strength; comparison requires two DISTINCT hosted-run identities so one run compared against itself is refused and a frozen export that binds no committed commit or tree can never corroborate; PROMOTION CONFIRMATION is strictly stronger than same-source and additionally requires that both runs qualified the authoritative target under an identical admitted Rust and Cargo toolchain and an identical admitted bootstrap Python runtime, in the same repository and the canonical authoritative workflow path, that each side presents an exact admitted evidence envelope, and that the external actions the qualification workflow uses are immutable-content pinned, so provenance equivalence is not promotion equivalence; spec/tier0_cross_run.rs owns the sealed comparator whose SameSourceProof comes only through compare_runs and whose PromotionConfirmationProof comes only through confirm_promotion over two verify-sealed qualifications, bootstrap/receiptcheck.rs independently recomputes each run's evidence and exposes a compare mode that runs both, bootstrap/seedcheck.rs executes both, and docs/36_PUBLIC_API_CI_AND_RELEASE.md owns the hosted-qualification posture |
| DEC-077 | LOCK | Architecture | G0/G3/G9 | Layered dynamic verification and anti-self-grading | Dynamic verification keeps basis method claim coverage enforcement-posture freshness and result as separate typed axes and no aggregate assurance ladder substitutes for them; the four verification bases ContractProjection IndependentReference DirectBoundary and RuntimeObservation remain distinct and a generated projection or a self-observation can never be the sole independent oracle; a ContractProjection generates test input and never serves as its own oracle or a voting witness yet its completeness still matters because a projection drives valid-action generation transition enumeration coverage runtime monitor schemas generated proof inputs and conformance replay so projection completeness is exact typed-authority to generated-projection parity while independent evidence is a separately owned model evaluator hostile boundary or relation; coverage strength is not an ordered ladder and coverage of a declared model is not coverage of its law so an exhaustive-within-declared-model pass is exhaustive only over the declared subset and carries no law-completeness claim; NonOscillation is a claim kind refusing a control or retry cycle that admits no new evidence and is distinct from Liveness BoundedResponse and Convergence; a single ordered enum spanning method coverage freshness and result is forbidden by shape and the executable shape guard is authored against the real types in a later phase; spec/verification.rs authors the typed basis as a field on the proof requirement so a projection satisfying an independent-evidence route is a compile error and docs/38_DYNAMIC_VERIFICATION_AND_CONFORMANCE.md owns the discipline; this refines SEED-INDEPENDENT-ORACLE and SEED-AVAILABILITY-AXES |
| DEC-078 | LOCK | Enforcement | G3/G6/G9 | Runtime conformance independent replay and no historical rewrite | Runtime verification may refuse before an admitted irreversible boundary or append conformance evidence but it never rewrites semantic law durable history or proof disposition; runtime conformance requires an independent replay witness so a monitor generated from a contract projection corroborates but never alone concludes conformant reusing the two-distinct-witness quorum proven by the cross-run promotion-confirmation work; runtime-observed coverage is intrinsically non-independent so a self-monitor may corroborate but never alone yield a conformant verdict; runtime evidence is append-only and a later conformance observation never edits an earlier admitted proof disposition; docs/38_DYNAMIC_VERIFICATION_AND_CONFORMANCE.md owns the conformance law; this refines DEC-075 |
| DEC-079 | LOCK | Architecture | G3/G8/G9 | Immutable candidate nursery and complete lineage | A candidate nursery persists immutable content-addressed lineage records comprising the candidate identity the patch or content commitment the source-frontier commitment the dependency commitments the lineage the generator or pilot evidence the qualification and holdout receipts and the reuse key while full speculative workspaces are disposable rematerializations; candidate origin is recorded truthfully including a machine-assisted-synthesis origin and a repair-of-prior-candidate origin so the architecture never lies about provenance and recording a synthesizer confers no authority; a candidate carries a realization posture of missing scaffold or candidate so a compiling stub that merely returns success is a scaffold structurally present and unqualified and counted in the unrealized denominator and a binary realized graph can never go green over a tree of stubs; candidate lineage reuses existing typed relation fields and mints no parallel candidate-evidence-relation edge vocabulary; the candidate lineage identity is orthogonal and never generalizes or replaces the kernel identity quartet KernelContractId KernelImplementationId QualifiedKernelId and KernelQualificationReceiptId or ContentDigest or ProgramImageId or a git commit so a specialized kernel candidate carries both a candidate lineage identity and a KernelImplementationId; spec/sprouting.rs authors the lifecycle and spec/identities.rs authors the candidate lineage identity and docs/39_SPROUTING_NURSERY_AND_PROMOTION.md owns the nursery law |
| DEC-080 | LOCK | Enforcement | G3/G8/G9 | RealizationPreserving versus LawChanging promotion | Promotion classifies a candidate change as realization-preserving or law-changing; a realization-preserving candidate may promote through a mechanical or bounded-search lane while a law-changing candidate routes to the architect and the DEC-074 proof-policy anti-weakening process; candidate repair authority is mechanical bounded-search or architect-required and there is no ordinary judge-repair lane a candidate may take on its own courtroom so when the judge itself is wrong through a drifted independent model a mis-specified fixture or a stale judge-root view the campaign stops as architect-required the judge or proof-policy amendment proceeds separately under DEC-074 affected evidence becomes stale and the campaign resumes from the new frozen judge snapshot; the architect-required outcome has a terminal disposition home so it can never silently drop from the denominator; a candidate never modifies its frozen judge or prior evidence; docs/39_SPROUTING_NURSERY_AND_PROMOTION.md owns the promotion law |
| DEC-081 | LOCK | Enforcement | G3/G8/G9 | Bounded search evaluation-set separation and transfer reuse | Every candidate search declares finite candidate logical-work memory and monotonic-deadline bounds and receipts the actual work performed; search qualification holdout and regression inputs carry explicit evaluation-set roles and holdout evidence for a campaign cannot reuse its search inputs; transfer reuse is keyed by a reuse key and requalified against upstream churn so a reused candidate unit is never blessed by stale evidence; candidate origin variants including deterministic generation bounded search transfer reuse machine-assisted synthesis repair of a prior candidate and human authorship are all recorded honestly and none confers authority; docs/39_SPROUTING_NURSERY_AND_PROMOTION.md owns the search and evaluation law |
| DEC-082 | LOCK | Architecture | G0/G3/G9 | Whole-tree speculative materialization and frontier qualification | A complete speculative implementation tree may be generated before implementation gates close but authority advances only through dependency-ordered qualification so a later green frontier can never bless an unresolved earlier frontier and the trusted frontier advances dependency-first; persistent immutable nursery records survive across promotions while full speculative workspaces are disposable rematerializations so the campaign earns compound interest through persisted records and keyed transfer reuse and invalidation rather than granting stale whole-tree bytes squatters rights and after a trusted-frontier promotion the loop recomputes transitive staleness preserves unaffected candidate units invalidates affected descendants and rematerializes the full workspace; the speculative crates tree lives in an external campaign root and never in the tracked checkout so the forbidden target paths are never tripped and the campaign root holds physically disjoint judge candidate evidence and nursery roots with the judge read-only where practical and isolation is the combination of physically disjoint roots write containment entry-type-aware before and after snapshots exact digest binding root-containment checks and evidence invalidation on judge change; digest binding detects and invalidates judge mutation so a candidate that mutated a judge produces evidence that cannot verify but it does not make filesystem writes unrepresentable; the judge root is a content-addressed frozen judge snapshot and every evidence artifact binds the exact judge digest it was produced under; the campaign closure graph must enter the closed generated-view registry before it may land; docs/39_SPROUTING_NURSERY_AND_PROMOTION.md owns the whole-tree topology; this refines DEC-048 and DEC-075 |
<!-- DECISION-LEDGER:END -->

## Reopening rule

A locked or killed decision reopens only through a signed amendment containing new evidence, affected obligations, compatibility impact, proof changes, migration plan, and explicit supersession. A code agent cannot reopen it through implementation convenience.
