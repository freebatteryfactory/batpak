# hostbat semver-check classification (FIRST PUBLISH at 0.9.0).
# hostbat has never been on crates.io, so the `cargo semver-checks` registry
# lookup is skipped — there is no prior published version to diff against.
package: hostbat
baseline_version: 0.9.0
current_version: 0.9.0
witness: target/semver-public-api/semver-checks.txt
summary: "First publish at 0.9.0; no prior crates.io baseline to diff."

public_api_baseline_removals: []
public_api_baseline_additions: []
accidental_leaks_hidden: []

entries: []
